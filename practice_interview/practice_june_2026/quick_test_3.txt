1)
#include <iostream>

int main() {
    int a = 5;
    int b = 10;

    int& r = a;

    r = b;

    std::cout << a << " " << b << std::endl;
}

2)
#include <iostream>

int main() {
    int x = 3;

    int* p = &x;

    (*p)++;

    std::cout << x << std::endl;
}

3)
#include <iostream>

class A {
public:
    A()  { std::cout << "C "; }
    ~A() { std::cout << "D "; }
};

int main() {
    A a;

    {
        A b;
    }

    std::cout << "E ";
}

4)
#include <iostream>

class Base {
public:
    void foo() {
        std::cout << "Base";
    }
};

class Derived : public Base {
public:
    void foo() {
        std::cout << "Derived";
    }
};

int main() {
    Base* p = new Derived();
    p->foo();
}

5)
#include <iostream>
#include <string>

int main() {
    std::string a = "Hello";
    std::string b = a;

    b[0] = 'Y';

    std::cout << a << " " << b;
}

6)
#include <memory>

int main() {
    std::unique_ptr<int> p1 =
        std::make_unique<int>(10);

    std::unique_ptr<int> p2;

    p2 = p1;
}

7)
#include <iostream>
#include <memory>

int main() {
    auto p = std::make_unique<int>(5);

    int* raw = p.get();

    *raw = 8;

    std::cout << *p;
}

8)
#include <iostream>
#include <vector>

int main() {
    std::vector<int> v = {1,2,3};

    for(auto x : v)
    {
        x *= 2;
    }

    std::cout << v[1];
}

9)
class Base {
public:
    virtual void foo() {}
};

class Derived : public Base {
public:
    ~Derived() {}
};

Base* p = new Derived();

delete p;

10)
#include <memory>

auto p1 = std::make_unique<int>(42);

auto p2 = std::move(p1);






1) 10 10
2) 4
3) C C D E D
4) Base (foo() is not virtual, so static dispatching)
5) Hello Yello
6) Can't compile as p1 is unique
7) Compiles and prints 8
8) 2 (the for loop is modifying a copy)
9) Base does not have virtual destructor
10) p2 owns 42, p1 is nullptr, only *p2 is valid